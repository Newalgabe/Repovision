{
  "name": "repovision",
  "version": "1.0.0",
  "description": "Drop any public GitHub repo URL — get a visual architecture map with plain-English explanations",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "vercel-build": "echo 'Done'"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "node-fetch": "^2.7.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
